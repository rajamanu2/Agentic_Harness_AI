# Salesforce DX Workspace

Place or clone the Salesforce DX project here.

The backend defaults `CODEX_DEFAULT_REPO_PATH` to `../salesforce` when it is run from `backend/`.
Set `CODEX_DEFAULT_REPO_PATH` if your Salesforce repository lives elsewhere.
